# Frontend Instructions

## Stack
- Vue 3 + TypeScript

## Rules
- Keep API field names aligned with backend contracts.
- Do not redesign unrelated pages during feature edits.
- Reuse existing composables/components/patterns before adding new abstractions.
- Keep state management and API call style consistent with current code.

## Output
- Return unified diff for touched frontend files only.
- Include build result only; keep summary brief.