export interface ActiveSession {
  id: string;
  goal: string;
  course: string;
  chapter: string;
  phase: string;
}

export interface StartLearningForm {
  goal: string;
  course: string;
  chapter: string;
}
