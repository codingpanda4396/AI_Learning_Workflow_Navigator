package navigator.domain.enums;

/**
 * 诊断题目维度，用于 questionId 映射与 profile 推导。
 */
public enum DiagnosisDimension {
    GOAL_SITUATION,
    FOUNDATION,
    GAP,
    WEAKNESS_SCOPE,
    PREFERENCE,
    EXECUTION_RISK
}
