package navigator.application.rule.planning;

import navigator.application.planning.PlanningContext;
import navigator.application.rule.engine.Rule;

public interface PlanningRule extends Rule<PlanningContext, String> {
}
