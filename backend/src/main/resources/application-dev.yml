spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ai_learning}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}

logging:
  level:
    com.panda.ainavigator: INFO

app:
  llm:
    enabled: ${LLM_ENABLED:false}
    fallback-to-rule: true
