spring:
  application:
    name: learning-workflow-navigator-backend

  datasource:
    url: jdbc:postgresql://8.145.55.7:5432/ai_learning_workflow_navigator
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 439695

  flyway:
    enabled: true
    locations: classpath:db/migration

  sql:
    init:
      mode: never

server:
  port: 8080

# CORS will be configured in code for /api

navigator:
  llm:
    enabled: false
    baseUrl: ${NAVIGATOR_LLM_BASE_URL:https://api.openai.com}
    apiKey: ${NAVIGATOR_LLM_API_KEY:}
    model: ${NAVIGATOR_LLM_MODEL:gpt-4.1-mini}
    timeoutMs: ${NAVIGATOR_LLM_TIMEOUT_MS:8000}
