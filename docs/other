# 开发任务执行页计划

## Summary
基于当前仓库现状，本次不按“从零做一个 execution page”推进，而是以现有 [TaskRunView.vue](D:/Panda_Code/AI_Learning_Workflow_Navigator/frontend/src/views/TaskRunView.vue) 和 learning scaffold engine 为基础，收束成一个更明确的“阶段化学习工作台”。目标是把执行页的产品表达、四阶段交互和 DFS/BFS 首个闭环统一起来，让页面更像 AI 学习工作流系统的核心工作台，而不是执行能力很多但表达分散的演示页。

本轮优先完成 DFS/BFS 的四阶段闭环展示与交互一致性，同时补齐文档，明确“规则骨架 + LLM 填充”的边界。后端已有 `StageScaffoldWorkbenchPayload`、四阶段 stage、validator/evaluator/tutor composer，可直接复用，不应重做一套新协议。

## Key Changes
- 前端以现有 [TaskRunView.vue](D:/Panda_Code/AI_Learning_Workflow_Navigator/frontend/src/views/TaskRunView.vue) 为主入口重构信息层级，不新开平行页面：
  - 顶部固定突出“当前知识点 / 当前阶段 / 阶段目标 / 进度”。
  - 主区只保留一个主任务焦点，压缩次要说明，避免像多卡片拼盘。
  - 右侧 Scaffold Panel 明确变成“阶段化认知动作库”，前两阶段显示理解型 scaffold，后两阶段显示表达/反思型 scaffold。
  - 反馈区统一成“教学反馈”而非“判题反馈”，前两阶段重点解释混淆点，第三阶段重点指出表达缺口，第四阶段重点总结错误模式。
- 内容与交互按四阶段统一：
  - `STRUCTURE`：LLM 简讲 + 无惩罚选择题 + 错因解释，目标是建立轮廓。
  - `UNDERSTANDING`：继续 explanation + 机制型选择题，目标是理解为什么这样工作。
  - `TRAINING`：切换为一句话解释、对比解释、原因解释等低压表达任务，反馈不打分，只给教学修正。
  - `REFLECTION`：系统汇总前序表现，给 1-2 个轻量反思动作，并产出可迁移策略。
- 前后端接口策略保持“少改协议，重做映射”：
  - 继续复用现有 `StageScaffold` / `StageScaffoldWorkbenchPayload`，前端做字段归一和展示重映射。
  - 若现有 payload 缺少页面必需信息，只补最小字段，例如阶段副标题、题目类型标识、反馈展示标签，不改主协议方向。
  - 不在本轮引入新的通用知识包引擎设计，先把 `ds_dfs_bfs` 跑成完整样板。
- 组件与模型层收束：
  - 优先复用现有 task-run 组件，不新增大量抽象层。
  - 把“阶段头部、主任务卡、表达区、反馈区、scaffold panel”的职责边界重新定清。
  - 将 DFS/BFS 的展示文案和 scaffold 配置集中，减少散落在多个 `constants` 文件中的重复口径。

## Public APIs / Interfaces
- 默认不新增新的主接口，继续使用：
  - `GET /api/tasks/{taskId}/learning-scaffold/stage/current`
  - `POST /api/tasks/{taskId}/learning-scaffold/action`
  - `POST /api/tasks/{taskId}/learning-scaffold/structure/skeleton`
  - `POST /api/tasks/{taskId}/learning-scaffold/structure/complete`
- 前端类型继续以 [scaffoldEngine.ts](D:/Panda_Code/AI_Learning_Workflow_Navigator/frontend/src/types/scaffoldEngine.ts) 为核心。
- 如需补充字段，限定在 `StageScaffoldWorkbenchPayload` 上做增量扩展，保证兼容现有 `TaskRunView` 和 `useLearningScaffoldEngine` 调用方式。

## Test Plan
- 页面结构验证：
  - 首屏必须一眼看到知识点、阶段、目标、进度和当前主动作。
  - 主学习区只能有一个主任务焦点，辅助说明不能压过操作引导。
  - 右侧 panel 在四阶段都能体现“当前该点什么、为什么点”。
- 四阶段流程验证：
  - STRUCTURE 可完成 explanation 查看、选择、反馈显示、scaffold 触发补充 explanation。
  - UNDERSTANDING 能复用结构但题目重心切到“为什么”。
  - TRAINING 能提交文本表达并收到教学型反馈。
  - REFLECTION 能显示系统总结、轻量反思动作和策略收口。
- 数据联调验证：
  - mock 数据先跑通 DFS/BFS 四阶段。
  - 再切真实接口，确认 stage 切换、action 提交、反馈回显和完成状态一致。
- 回归重点：
  - 不破坏现有 task run 主链路、session 恢复、reflection summary 展示和旧 execution flow。
  - 响应式下移动端仍能清楚看到当前阶段与主动作。
  - 加载、空态、错误态都保持产品化表达，不退回通用聊天壳。

## Assumptions
- 本轮目标是“执行页产品化收束 + DFS/BFS 首个完整闭环”，不是通用多知识包平台化改造。
- 现有 scaffold engine 和后端四阶段能力足够支撑 v1，优先复用，不重写后端主流程。
- `docs/` 下目前仅有现状文档，用户提到的四份文档尚未真正落库，应作为本轮第一批产出。
- 实施顺序按：文档定规格 -> 页面骨架收束 -> DFS/BFS mock 闭环 -> 四阶段补齐 -> 最后联调真实接口。
