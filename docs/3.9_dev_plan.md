# 3.9 16:12现状
已经完成：
- 用户认证与学习会话主链路

- 四阶段学习流程

- AI Tutor MVP

- 个性化路径规划雏形

- Rule 保底 + LLM 增强 + fallback 的稳健架构

目前基本可用，但还是存在**链路未闭环**的问题（缺少测验、薄弱点诊断、学习路径反向调整）

# 下一阶段目标
构建“路径规划—内容学习—习题训练—结果评估—动态调整”的闭环学习引擎。
1. 习题训练进入主链路
2. 训练结果加入个性化规划
3. 从任务执行系统升级为**训练驱动的学习系统**

# 演进后的产品形态
1. 学习路径层
决定要以什么样的成果要求来学习（LLM综合所有信息来决定）
2. 学习任务层
按照我们的四阶段模型
STRUCTURE
UNDERSTANDING
TRAINING
EVALUATION
把要求拆成具体可执行的任务
3. 习题训练层
用LLM生成题目，目的是检测学习成果、排查薄弱点
4. 学习反馈层
根据用户的“学习状况”，智能总结学习报告并反哺前面的学习路径，完成闭环。
# 实现prompt

```
可以。下面我直接给你一套**分步骤、可直接丢给 Codex/Cursor 执行的技术方案 Prompt 包**。
目标是把你当前系统从：

> 可用主链路 + LLM 增强

推进到：

> **训练驱动的闭环学习引擎**

我会按**实施顺序**来拆，每个 Prompt 都尽量满足你现在的工程现实：

* 不推翻现有架构
* 延续 Rule 保底 + LLM 增强 + fallback
* 先做最小闭环，再做增强
* 便于你逐步提交、逐步联调、逐步验收

---

# 总执行顺序

建议你按这个顺序给 Codex 跑：

1. **Prompt 01：先做总体设计扫描与实施计划**
2. **Prompt 02：实现习题训练数据模型与后端落库**
3. **Prompt 03：实现训练题生成与获取接口**
4. **Prompt 04：实现用户作答、判题、结果持久化**
5. **Prompt 05：实现薄弱点诊断与掌握度模型**
6. **Prompt 06：把训练反馈接入个性化路径规划**
7. **Prompt 07：生成学习反馈报告接口**
8. **Prompt 08：前端最小闭环联调**
9. **Prompt 09：补测试、日志、灰度与回退机制**
10. **Prompt 10：文档收敛与技术方案沉淀**

---

# Prompt 01：总体设计扫描与落地实施计划

先让 Codex不要急着写代码，先基于当前仓库做**现状扫描 + 差异分析 + 实施方案**。

```text
你现在的角色是：

1. 高级 Java 后端架构师
2. Spring Boot 3 / JDK17 / PostgreSQL / Flyway 工程顾问
3. 学习流程系统建模专家
4. 渐进式重构与 MVP 演进专家

项目当前背景如下：

【当前已完成】
- 用户认证与学习会话主链路
- 四阶段学习流程：STRUCTURE / UNDERSTANDING / TRAINING / EVALUATION
- AI Tutor MVP
- 个性化路径规划雏形
- Rule 保底 + LLM 增强 + fallback 的稳健架构

【当前问题】
系统虽已基本可用，但学习链路尚未闭环：
- 缺少结构化习题训练
- 缺少训练结果沉淀
- 缺少薄弱点诊断
- 缺少基于训练反馈的学习路径反向调整
- 缺少学习报告与反馈闭环

【下一阶段目标】
构建“路径规划—内容学习—习题训练—结果评估—动态调整”的闭环学习引擎。

【期望的四层产品结构】
1. 学习路径层：根据目标、掌握度、错误模式决定学习要求与路径
2. 学习任务层：按四阶段模型拆分为可执行任务
3. 习题训练层：通过 LLM 生成/组织题目，用于检测学习成果与排查薄弱点
4. 学习反馈层：根据学习状况生成报告并反哺路径规划

你的任务不是立刻大改代码，而是：
先基于当前仓库做“现状扫描 + 差异分析 + 可执行实施计划”。

请严格输出以下内容：

1. 当前项目中与“训练闭环”最相关的现有模块清单
   - controller
   - service
   - repository
   - domain model
   - migration
   - prompt template
   - frontend page/store/api
2. 为实现下一阶段目标，建议新增/修改的核心模块
3. 推荐的数据模型设计（最小可行版本）
4. 推荐的 API 设计（最小可行版本）
5. 推荐的实施顺序（按提交批次拆分）
6. 每一步可能影响的现有代码与风险点
7. 最后给出一个“建议先做的第一个提交”的具体范围

要求：
- 严格基于当前项目已有结构做增量设计
- 不要推翻现有四阶段模型
- 继续沿用 Rule 保底 + LLM 增强 + fallback 思路
- 以最小闭环可交付为目标
- 输出要具体到类、表、接口、文件级别
- 如发现命名不一致、文档编码问题、潜在重复职责，也请指出
```

---

# Prompt 02：实现习题训练数据模型与落库

这一步是最关键的地基。先把“题目”和“作答”结构化，不然闭环永远做不起来。

```text
你现在的角色是：

1. Spring Boot 3 / JDK17 / PostgreSQL / Flyway 高级后端工程师
2. DDD 风格业务建模顾问
3. 学习系统中的训练模块实现专家

请基于当前仓库，实现“习题训练层”的最小可行后端数据模型。

目标：
把 TRAINING 阶段从“静态内容展示”升级为“可生成、可获取、可作答、可统计”的结构化训练系统。

请完成以下工作：

【一、数据库设计】
新增 Flyway migration，创建最小必要表：

1. practice_item
建议字段至少包括：
- id
- session_id
- task_id
- user_id
- node_id
- stage
- question_type
- stem
- options_json（可空）
- standard_answer
- explanation
- difficulty
- source（RULE / LLM / MANUAL）
- status
- created_at

2. practice_submission
建议字段至少包括：
- id
- practice_item_id
- session_id
- task_id
- user_id
- user_answer
- score
- is_correct
- error_tags_json
- feedback
- submitted_at

要求：
- 合理主键、外键、索引
- 与 session/task/user 关系清晰
- 保证按 session + task 查询性能
- 字段命名风格与现有表保持一致
- 尽量兼容未来扩展（如 token、latency、trace_id）

【二、后端领域建模】
新增/修改以下层次的代码：
- domain model
- enum
- repository interface
- jdbc/mybatis repository implementation（按你当前项目风格）
- service skeleton

建议至少包含：
- PracticeItem
- PracticeSubmission
- PracticeItemStatus
- PracticeItemSource
- PracticeQuestionType
- PracticeService（先定义接口/骨架）
- PracticeRepository
- PracticeSubmissionRepository

【三、与现有业务对齐】
要求检查并对齐：
- task 与 session 归属关系
- user/session 越权访问校验风格
- TRAINING 阶段与 practice item 的绑定方式
- 是否需要通过 node_id 辅助后续掌握度统计

【四、产出要求】
请直接修改代码并保证可编译。
完成后请输出：
1. 新增/修改文件清单
2. 表结构说明
3. 核心设计说明
4. 后续下一步建议（只写最值得做的一步）

注意：
- 这是最小可行版本，不要一次性过度设计复杂题型引擎
- 不要破坏现有 task/session 主链路
- 优先保证“先能跑通，再逐步增强”
```

---

# Prompt 03：实现训练题生成与获取接口

这一步让 TRAINING 阶段真正拿到题。

```text
你现在的角色是：

1. 高级 Java 后端工程师
2. LLM 结构化输出接入工程师
3. 学习系统训练题生成模块实现顾问

请基于当前项目，在已存在的四阶段任务流基础上，实现“训练题生成与获取”的最小闭环。

目标：
- 当任务进入 TRAINING 阶段时，后端可以为当前 task 生成或获取习题
- 题目可来自 Rule 或 LLM
- 继续坚持 Rule 保底 + LLM 增强 + fallback

请完成以下工作：

【一、服务设计】
新增或完善 PracticeService，至少包含以下能力：
1. listPracticeItems(sessionId, taskId, userId)
2. generatePracticeItems(sessionId, taskId, userId)
3. getOrCreatePracticeItems(sessionId, taskId, userId)

要求：
- 优先复用已生成题目，避免重复生成
- 当前 task 必须属于当前 session 且属于当前用户
- 仅允许对 TRAINING 阶段任务生成习题；若不是 TRAINING 阶段，给出合理业务异常

【二、题目生成策略】
实现双通道：
1. Rule 生成器（保底）
   - 可先基于 node title / task objective / stage content 生成简单题目
   - 至少支持 2~3 种基本题型，例如：
     - 单选题
     - 判断题
     - 简答题
2. LLM 生成器（增强）
   - 输出结构化 JSON
   - 必须经过 schema 校验 / 业务校验
   - 失败则自动 fallback 到 Rule

建议新增：
- PracticeGenerator
- RulePracticeGenerator
- LlmPracticeGenerator
- PracticeGeneratorRequest
- PracticeGeneratorResult
- Prompt template: PRACTICE_GENERATION_V1
- parser / validator

【三、API 层】
新增接口：
1. GET /api/session/{sessionId}/tasks/{taskId}/practice-items
   - 返回当前 task 的题目列表
2. POST /api/session/{sessionId}/tasks/{taskId}/practice-items/generate
   - 触发生成（如已有则按策略决定是否复用）

返回结构要清晰，至少包含：
- item id
- question type
- stem
- options
- difficulty
- source
- status

【四、日志与事件】
建议记录：
- 生成来源 RULE/LLM
- LLM 解析是否成功
- fallback 是否触发
- 生成数量
- 关键 prompt version / model / provider（若当前工程已有风格，保持一致）

【五、产出要求】
请直接修改代码并保证：
- 编译通过
- 接口可运行
- 若已有测试体系，请补最基本测试

完成后输出：
1. 新增/修改文件清单
2. 接口说明
3. 生成链路说明
4. fallback 触发条件说明
```

---

# Prompt 04：实现用户作答、判题、结果持久化

这是闭环的核心动作。没有 submit，前面都只是“展示题目”。

```text
你现在的角色是：

1. 高级 Java 后端工程师
2. 学习系统训练判题模块实现专家
3. 结构化评估与结果持久化顾问

请在当前项目中实现“用户作答 -> 判题 -> 结果持久化”的最小闭环。

目标：
让 practice item 不只是展示，而是可以被真正提交答案、完成判定，并形成训练结果数据。

请完成以下工作：

【一、服务能力】
完善 PracticeService 或新增 SubmissionService，至少支持：
1. submitPracticeAnswer(sessionId, taskId, practiceItemId, userId, request)
2. listPracticeSubmissions(sessionId, taskId, userId)

【二、判题逻辑】
先实现最小可行判题规则：
- 单选题：标准答案精确匹配
- 判断题：标准答案精确匹配
- 简答题：先做规则判定（关键词/标准答案弱匹配）或保留为人工/LLM 评估占位

要求：
- 输出 score
- 输出 is_correct
- 输出 feedback
- 输出 error_tags_json（可先按规则生成最小版本）

建议 error tags 先支持：
- CONCEPT_CONFUSION
- MISSING_STEPS
- CALCULATION_ERROR
- ANSWER_INCOMPLETE
- CARELESS_MISTAKE

【三、API 层】
新增接口：
1. POST /api/session/{sessionId}/tasks/{taskId}/practice-items/{practiceItemId}/submit
2. GET /api/session/{sessionId}/tasks/{taskId}/practice-submissions

请求体建议：
{
  "user_answer": "..."
}

返回建议：
{
  "submission": {...},
  "practice_item": {...},
  "judgement": {
    "score": 80,
    "is_correct": false,
    "feedback": "...",
    "error_tags": [...]
  }
}

【四、状态联动】
请考虑：
- practice_item status 是否需要从 GENERATED -> ANSWERED / COMPLETED
- task 是否需要记录训练完成度
- 是否为后续 mastery 统计提供事件或汇总入口

【五、测试与边界】
请至少覆盖：
- 非本人 session/task 越权提交
- 非法 practiceItemId
- 重复提交策略（允许多次 or 仅最后一次有效，按你建议实现）
- 非 TRAINING 任务是否允许提交题目答案

【六、产出要求】
请直接修改代码并保证可运行。
完成后输出：
1. 新增/修改文件清单
2. 判题规则说明
3. 状态流说明
4. 后续适合继续做的下一步建议
```

---

# Prompt 05：实现薄弱点诊断与掌握度模型

这一步会把训练结果转成“系统 intelligence”。

```text
你现在的角色是：

1. 学习分析系统建模专家
2. Java 后端架构师
3. 知识点掌握度与薄弱点诊断模块实现顾问

请基于当前仓库，新增“学习反馈层”的基础能力：知识点掌握度模型 + 薄弱点诊断。

目标：
把训练结果、评估结果、错误标签沉淀为结构化学习反馈，为路径规划与学习报告提供数据基础。

请完成以下工作：

【一、数据库设计】
新增 Flyway migration，创建或新增以下结构（按当前项目风格择优）：

方案优先建议：
1. node_mastery
字段建议：
- id
- user_id
- session_id（可选，若你认为需要）
- node_id
- mastery_score
- training_accuracy
- recent_error_tags_json
- latest_evaluation_score
- attempt_count
- updated_at

如果你认为更适合做汇总表 + 事件表，也可以，但必须解释原因。

【二、服务设计】
新增：
- MasteryService
- WeakPointDiagnosisService

至少支持：
1. recalculateNodeMastery(sessionId, taskId, userId)
2. getNodeMastery(sessionId, userId, nodeId)
3. diagnoseWeakPoints(sessionId, userId)

【三、最小可行算法】
请先采用规则化模型，不要引入复杂 KT 算法。
建议参考：
- 训练正确率高 -> mastery_score 上升
- 连续错误 / 相同错误标签重复 -> mastery_score 下降
- evaluation 分数高 -> mastery_score 加权提升
- Tutor 高频求助可预留扩展点，但当前先不强耦合

请明确写出你采用的计算规则，保证可解释。

【四、输出能力】
新增一个最小查询接口，例如：
GET /api/session/{sessionId}/learning-feedback/weak-points

返回内容至少包括：
- weak nodes
- mastery scores
- recent error tags
- diagnosis summary

【五、要求】
- 与当前 node/task/session 结构对齐
- 结果可被后续路径规划直接消费
- 代码风格与当前项目保持一致
- 尽量补基础单元测试或集成测试

完成后请输出：
1. 新增/修改文件清单
2. mastery 模型说明
3. 诊断规则说明
4. 可供路径规划消费的数据结构建议
```

---

# Prompt 06：把训练反馈接入个性化路径规划

这一段直接打通你现在最想要的“反哺”。

```text
你现在的角色是：

1. 高级 Java 架构师
2. LLM 个性化路径规划模块顾问
3. 训练反馈驱动的学习路径系统实现专家

项目中已经存在个性化路径规划雏形，包括：
- plan / plan-preview 相关接口
- PlanMode / PlanSource
- PersonalizedPathPlannerService
- Rule 保底 + LLM 增强 + fallback
- SmartBreakdown / goal diagnosis
- Prompt 输出校验与回退机制

现在请把“训练反馈”真正接入个性化路径规划，使规划不只依赖目标文本，还能基于用户真实训练表现动态调整。

请完成以下工作：

【一、上下文增强】
扩展 PersonalizedPathContext，使其真正纳入：
- mastery_by_node
- recent_error_tags
- recent_scores
- weak_points_summary

如果当前已有字段是占位，请将其做实，接入真实服务数据。

【二、规划策略增强】
在 Rule Planner / LLM Planner 中支持以下规则：
1. 低掌握度节点优先级前移
2. 对错误集中的节点允许插入补救任务
3. 高掌握度节点可降低训练密度
4. 对明显的 MISSING_STEPS / CONCEPT_CONFUSION 节点优先插入 UNDERSTANDING 或 TRAINING 补强任务

【三、约束与回退】
继续保持：
- 只能引用当前 chapter 节点
- stage 必须合法
- 插入任务数量受控
- 任何异常自动 fallback 到 baseline rule plan

【四、API 输出增强】
增强 plan-preview 或相关返回，至少体现：
- 当前规划来源 RULE / LLM / AUTO
- 哪些节点因训练反馈被前移
- 哪些补救任务被插入
- 风险标记与摘要说明

【五、要求】
- 不要推翻现有个性化路径模块
- 只做增量增强
- 尽量补测试，尤其关注 fallback 与 validator
- 保证编译通过

完成后请输出：
1. 新增/修改文件清单
2. PersonalizedPathContext 的新增字段与来源
3. 训练反馈如何影响规划的具体规则
4. fallback 触发条件与行为说明
```

---

# Prompt 07：生成学习反馈报告接口

这是“闭环可感知”的关键。对用户、前端、评委都很重要。

```text
你现在的角色是：

1. 学习分析产品后端工程师
2. 学习报告系统设计顾问
3. Java / Spring Boot API 设计师

请基于当前项目实现“学习反馈报告”最小版本接口，用于向前端返回当前 session 的学习诊断结果。

目标：
给用户一个可感知的学习反馈出口，也为前端的学习报告面板提供数据。

请完成以下工作：

【一、服务能力】
新增 LearningFeedbackReportService，聚合以下信息：
- 当前 session 基本信息
- 当前 chapter / node 学习进度
- 训练统计（题数、正确率、最近提交情况）
- 薄弱点诊断
- node mastery 概览
- 个性化路径调整摘要
- 下一步建议

【二、API】
新增接口：
GET /api/session/{sessionId}/learning-feedback/report

返回结构建议：
{
  "session_id": ...,
  "overall_progress": {...},
  "training_summary": {...},
  "weak_points": [...],
  "mastery_overview": [...],
  "path_adjustment_summary": {...},
  "next_actions": [...]
}

【三、生成策略】
优先采用规则汇总生成 report。
如果当前项目中已有 LLM summary 能力，可额外提供一个 llm_summary 字段，但必须：
- 可选
- 有严格 fallback
- 失败不影响主报告结构

【四、要求】
- 保持 API 稳定、字段语义清晰
- 不追求花哨，优先输出结构化可展示数据
- 保证后续前端可以直接消费
- 编译通过，并补最基本测试

完成后输出：
1. 新增/修改文件清单
2. report 返回结构说明
3. 汇总逻辑说明
4. 前端最适合如何展示的建议
```

---

# Prompt 08：前端最小闭环联调

后端做完后，前端要把“训练闭环”展示出来。

```text
你现在的角色是：

1. Vue 前端工程师
2. 学习产品交互设计顾问
3. 与 Spring Boot API 联调专家

请基于当前前端项目，实现“训练驱动学习闭环”的最小前端联调版本。

已知后端将提供或已提供：
- 获取训练题列表
- 生成训练题
- 提交题目答案
- 获取训练提交记录
- 获取 learning feedback report
- 个性化 plan-preview / path summary

目标：
在不推翻当前页面结构的前提下，让用户在任务页或会话页完成最小闭环：
查看题目 -> 作答 -> 查看判题结果 -> 看薄弱点与下一步建议。

请完成以下工作：

【一、API 与 store】
新增或完善：
- practice api
- learning feedback api
- practice store / session store 对应状态

【二、页面改造建议】
优先改造当前任务执行页（如 TaskRunView.vue）：
1. 在 TRAINING 阶段展示训练题面板
2. 支持题目加载、空状态、生成、提交答案
3. 展示判题结果、反馈、错误标签
4. 在页内或 session 侧边区域展示最小学习反馈摘要

如果更合适，也可在 session overview 页增加“学习反馈卡片”。

【三、交互要求】
- 不破坏当前原有学习主链路
- 保持现有 UI 风格
- 优先做最小可用，不追求复杂动画
- 需要有 loading / empty / error / retry 状态
- 对 TRAINING 阶段之外做合理隐藏或提示

【四、产出要求】
请直接改代码并保证前端可运行。
完成后输出：
1. 新增/修改文件清单
2. 页面交互说明
3. 状态流转说明
4. 与后端接口对接点说明
```

---

# Prompt 09：测试、日志、灰度与回退机制补强

这一步是把“能跑”变成“稳”。

```text
你现在的角色是：

1. 稳定性工程顾问
2. Java 测试工程师
3. AI 增强系统灰度发布方案设计师

请对当前新增的“训练驱动闭环能力”做稳定性补强，重点围绕：
- 测试
- 日志
- fallback
- 灰度开关
- 指标埋点

请完成以下工作：

【一、测试补强】
优先补以下测试：
1. practice item 生成
2. fallback：LLM 失败 -> Rule 成功
3. submit 判题
4. mastery 重算
5. personalized path 吃到训练反馈后的行为
6. 越权与非法参数测试

【二、日志与审计】
为以下关键链路增加日志：
- 题目生成来源与耗时
- 结构化解析成功/失败
- fallback 是否触发
- submission 判题结果
- mastery 更新前后变化
- path planner 输入摘要与输出摘要

要求避免打印敏感大文本，但要足够排障。

【三、灰度开关】
若项目已有配置体系，请为以下能力增加开关：
- llm practice generation enabled
- llm short answer judgement enabled
- feedback-driven planner enabled
- llm report summary enabled

【四、指标建议】
请实现或至少预留指标埋点位置：
- practice generation success rate
- parse success rate
- fallback rate
- submission count
- training accuracy
- weak point hit count
- plan personalization hit rate

【五、产出要求】
请直接改代码并输出：
1. 新增/修改文件清单
2. 新增测试说明
3. 日志与指标说明
4. 灰度建议
```

---

# Prompt 10：文档收敛与技术方案沉淀

最后收口，方便比赛文档和团队协作。

```text
你现在的角色是：

1. 技术文档架构师
2. 后端设计文档整理顾问
3. 面向团队协作的工程文档维护者

请基于当前仓库与本次新增实现，对“训练驱动的闭环学习引擎”做文档收敛。

目标：
形成一套可供团队内部协作、后续维护、比赛展示复用的技术文档。

请完成以下工作：

【一、文档整理】
新增或更新文档，建议包括：
1. 训练系统实现说明
2. 掌握度与薄弱点诊断说明
3. 训练反馈驱动的个性化路径规划说明
4. learning feedback report 接口说明
5. 前后端联调说明

【二、文档内容要求】
每份文档尽量包含：
- 目标
- 核心设计
- 数据模型
- API
- 状态流
- fallback 机制
- 后续扩展点

【三、编码与格式】
- 统一 UTF-8
- 清理历史乱码/编码问题
- 命名与术语统一，例如：
  - 学习路径
  - 学习任务
  - 习题训练
  - 学习反馈
  - 掌握度
  - 薄弱点

【四、最终产出】
请输出：
1. 新增/修改文档清单
2. 术语统一建议
3. 还缺失的文档空白点
4. 推荐给下一位开发者继续接手的入口说明
```

---

# 你现在最推荐的实际执行方式

我建议你不要一次把 10 个都扔进去。
按这个节奏最稳：

### 第一批

* Prompt 01
* Prompt 02

### 第二批

* Prompt 03
* Prompt 04

### 第三批

* Prompt 05
* Prompt 06

### 第四批

* Prompt 07
* Prompt 08

### 第五批

* Prompt 09
* Prompt 10

这样你每一批都能形成一个相对完整的 commit 组。

---

# 如果你只想先做最关键的 3 个

那就先跑这三个：

1. **Prompt 02：训练数据模型**
2. **Prompt 04：作答判题闭环**
3. **Prompt 05：掌握度与薄弱点诊断**

因为这三个做完，你的系统就第一次真正有了：

> 题目 -> 作答 -> 诊断

这个核心闭环。

---

# 最后，我帮你压缩成一句项目级目标

你后面可以一直拿这句话对齐团队：

> 下一阶段不是继续堆 AI 功能，而是把训练行为结构化，把训练结果反馈到路径规划中，形成“训练驱动的个性化学习闭环”。

如果你要，我下一条可以继续给你一份
**“适合直接放到 TODO.md 的开发拆分版”**，按后端 / 前端 / 文档 / 测试四列拆好。

```



1. 安装SQLServer与Navicat并连接
2. 建立数据库
3. 脚本创建Student数据库
4. 删除Student数据库
5. 脚本创建数据库表Student_DATA
6. 创建索引
7. 完成索引作业

云服务器内存过小，后续实验用mysql完成
