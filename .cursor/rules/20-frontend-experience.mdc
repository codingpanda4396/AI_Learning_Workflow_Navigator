---
description: 前端规则。约束 AI 学习导航系统前端页面的信息结构、状态呈现、个性化表达与闭环体验。
globs:
  - frontend/**
---

# 前端定位

前端不是“把接口字段摆出来”，而是把学习闭环转化为低负担、可理解、可推进的产品体验。

页面必须服务主链路：

**目标输入 → 用户诊断 → 学习规划 → 任务执行 → 反馈与 Next Action**

# 页面总原则

1. 页面先表达“当前用户该做什么”，再展示细节
2. 页面要体现系统在理解用户，而不是机械展示字段
3. 页面文案必须统一中文，不允许中英文混杂
4. 页面层级要清楚：当前结论 > 原因解释 > 可执行动作 > 补充信息
5. 一个页面只突出一个核心任务

# UI 体验原则

必须优先保证：

- 关键信息一屏可见
- 当前步骤明确
- 下一步动作明确
- 解释信息可展开，不压主流程
- 空状态、加载态、错误态完整

避免：

- 一上来堆很多字段
- 卡片很多但主次不明
- 解释文案太长、像技术日志
- 页面像调试台而不是产品页

# 目标输入页要求

目标输入页要帮助用户把模糊目标说清楚。

必须突出：

- 学什么
- 学到什么程度
- 时间约束
- 当前基础
- 偏好 / 紧迫度

输入项应尽量结构化，减少用户负担。

# 诊断页要求

诊断页不是问卷系统，不要做得像考试。

必须体现：

- 当前是在“补齐规划所需信息”
- 问题数量轻量、目标明确
- 每道题都在服务后续个性化规划
- 页面上能感知“系统正在判断你的当前状态”

不要出现纯技术枚举词直接暴露给用户。

# 规划页要求

规划页的核心不是“展示一堆任务”，而是让用户理解：

- 从哪里开始
- 为什么这样开始
- 接下来怎么推进

规划页至少要清楚表达：

- 推荐入口
- 推荐策略
- 当前阶段目标
- 本轮任务安排
- 为什么是这个方案
- 用户可确认的下一步

必须避免：

- 只有建议，没有理由
- 看起来很个性化，但没有真实差异
- 页面结构像接口 dump

# 执行页要求

执行页必须体现“学习脚手架”，而不是自由聊天。

页面上应清楚表达：

- 当前任务是什么
- 这一步要完成什么
- 推荐如何向 LLM 提问
- 什么时候算完成
- 完成后会如何反馈到系统

必须有任务目标、提问提示、完成条件、留痕入口。

# 反馈页要求

反馈页必须回答三个问题：

- 这轮学得怎么样
- 为什么会是这个结果
- 接下来最合理的动作是什么

必须突出：

- 本轮结果
- 关键证据
- 主要问题 / 风险
- next action

不要只做成“恭喜完成”或“任务统计页”。

# 个性化呈现要求

前端要把个性化体感做出来，但不能伪个性化。

只有在以下信息真实影响结果时，才展示相关表达：

- 当前基础
- 当前障碍
- 风险标签
- 偏好约束
- 时间预算

不要为了显得聪明而拼接一些泛化句子。

# 工程要求

前端改动时：

1. 优先复用既有路由、store、组件模式
2. 保持页面命名与主链路一致
3. 页面文案全部中文统一
4. 修改后至少检查：
   - 编译是否通过
   - 路由是否可达
   - 关键状态是否正常渲染
   - 明显布局问题是否修复

如果涉及重大页面调整，应先给出信息结构方案，再编码实现。
