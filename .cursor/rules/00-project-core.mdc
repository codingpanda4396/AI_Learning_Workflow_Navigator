---
description: 项目总则。适用于整个 AI 学习导航系统项目，统一产品边界、主链路、对象口径与开发优先级。
alwaysApply: true
---

# 项目定义

本项目不是通用 AI 问答工具，也不是自由聊天式学习产品。
本项目是：**面向计算机学习场景的 AI 学习导航系统 MVP**。

系统唯一主线固定为：

**目标输入 → 用户诊断 → 学习规划 → 任务执行 → 反馈与 Next Action**

任何实现、重构、命名、页面、接口设计，都必须服务这条主链路。

# 产品边界

必须坚持以下边界：

- 不扩展为多学科通用平台
- 不扩展为长周期课程运营系统
- 不扩展为高自由度开放聊天产品
- 不扩展为纯内容生成器或答案生成器
- 不为了“看起来更智能”而破坏闭环稳定性

如果某项改动不能直接增强主链路闭环，就不要做。

# 核心原则

1. **先闭环，后增强**
   - 优先打通最小可运行链路
   - 先保证对象流转成立，再增强智能度与 UI 细节

2. **规则主导，LLM 辅助**
   - 规则系统负责主决策、结构约束、枚举选择、状态流转
   - LLM 负责解释增强、文案润色、导师对话脚手架、报告总结
   - 不允许把关键业务决策完全交给 LLM 自由生成

3. **合同先行，内部可替换**
   - 先稳定核心对象、枚举、DTO、API 出入参
   - 在不破坏合同的前提下，可以替换模块内部实现

4. **纵向切片优先，不做横向铺满**
   - 优先做一条完整链路
   - 不要先把每个模块都做成半成品

5. **最小增量改动**
   - 尽量复用现有包结构、已有对象和既有接口风格
   - 非必要不大规模重命名、不大范围迁移文件

# 冻结主对象

以下对象属于系统核心语义，优先复用，不要重复发明近义对象：

- `LearningGoalInput`
- `StructuredLearningGoal`
- `GoalContextSnapshot`
- `LearnerProfileSnapshot`
- `DiagnosisEvidenceSummary`
- `LearningPlanPreview`
- `TaskBlueprint`
- `TaskExecutionRecord`
- `ExecutionEvidenceSummary`
- `LearningReport`
- `NextActionDecision`

如果已有对象能表达语义，优先扩展现有对象，不要新增语义重叠对象。

# 冻结链路要求

每一环都必须满足：

- **可结构化输入**：不能只传大段自然语言
- **可解释输出**：关键决策必须能说明原因
- **可供下游消费**：输出不能只面向 UI
- **可留痕**：执行与反馈阶段必须沉淀证据

# 决策与展示分离

实现时必须区分两类结果：

1. **系统决策对象**：供下游模块继续消费
2. **用户展示对象**：供页面展示和解释

不要把“展示文案”当成“真实决策”。
不要从解释文案反推结构化字段。

# 对 Cursor 的工作要求

每次任务都要遵守：

- 先阅读相关上下文文件，再改代码
- 先理解当前模块在主链路中的位置，再下手实现
- 保持命名统一、语义统一、枚举统一
- 修改后进行最小必要验证
- 输出修改文件清单、影响范围、验证结果

如果需求跨度过大，应先产出实施计划，再开始编码。
