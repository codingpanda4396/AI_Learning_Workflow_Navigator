---
description: 后端规则。约束 AI 学习导航系统后端的领域建模、接口设计、包结构、规则决策与 LLM 集成方式。
globs:
  - backend/**
---

# 后端定位

后端不是简单接口集合，而是主链路的编排与决策中枢。
后端代码必须围绕“目标输入 → 诊断 → 规划 → 执行 → 反馈”构建清晰的数据流。

# 领域建模原则

1. 优先表达业务语义，不要只按页面字段堆 DTO
2. DTO、Domain、ViewModel 语义分层清晰
3. 枚举优先于自由文本
4. 对外接口字段要稳定、可解释、可延续
5. 相同语义只保留一个主表达

# 包结构原则

优先沿用现有包结构与命名风格。
如无强烈必要，不要大规模重构目录。

推荐保持类似分层：

- `api`：Controller / Request / Response
- `application`：编排服务、Assembler、Facade
- `domain`：核心模型、规则、决策对象
- `infrastructure`：持久化、外部依赖、LLM 接入

# 接口设计原则

所有接口都应满足：

- 请求与响应字段语义清晰
- 避免中英文混杂字段
- 避免把页面文案直接硬编码进领域对象
- 能体现当前阶段的主状态
- 能被下游模块继续消费

不要返回“只有页面能看懂、系统无法继续使用”的松散字段。

# 规则与 LLM 的分工

必须坚持：

- 规则系统负责候选生成、风险判断、枚举决策、状态机流转
- LLM 负责解释增强、可读性优化、导师话术生成、报告摘要生成
- 关键业务结果必须可以在没有 LLM 的情况下退化运行

禁止：

- 让 LLM 直接自由生成最终 strategy code
- 让 LLM 决定核心状态流转
- 从 LLM 文案中反向提取主决策字段作为真实结果

# 诊断模块要求

诊断模块是“当前目标下的状态识别器”，不是人格分析器。

实现时注意：

- 基于 `StructuredLearningGoal` / `GoalContextSnapshot` 裁剪诊断范围
- 输出的是 `LearnerProfileSnapshot`，不是永久人格画像
- 诊断结果必须进入后续规划控制变量
- 诊断证据要结构化，避免全靠自由文本

# 规划模块要求

规划模块是“受约束的个性化决策器 + 任务编排器”。

必须保证：

- 规划真正消费用户画像
- 输出可结构化执行，不只是建议文案
- 推荐入口、策略、阶段、任务、检查点、成功标准清晰
- 解释字段与主决策字段一致

不要出现：

- 画像存在，但不影响规划
- 推荐策略与证据脱节
- 解释很个性化，但结构化结果很通用

# 执行模块要求

执行模块不是自由聊天页，而是：

- 学习行为编排层
- 对话脚手架层
- 执行证据采集层

必须体现：

- 当前任务目标
- 当前任务类型
- 当前推荐提问框架
- 完成标准
- 执行证据记录

不要只靠“点击完成”表示任务完成。

# 反馈模块要求

反馈模块是“结果编译层 + 归因层 + 下一步调度层”。

必须同时输出：

- 用户可读报告
- 系统可消费的 `NextActionDecision`

不要把反馈页做成纯总结文案页。

# 持久化与证据

任何与执行和反馈相关的关键结果，优先结构化存储：

- 输入快照
- 诊断结果
- 规划结果
- 任务执行记录
- 证据摘要
- 报告摘要
- next action 决策

允许存在 explanation 文案，但 explanation 不能替代结构化字段。

# 改动纪律

进行后端修改时：

1. 先定位当前改动处在主链路哪一环
2. 先检查是否已有可复用对象、枚举、assembler
3. 尽量做增量改动，不破坏现有 contract
4. 完成后至少执行：编译 / 单测 / 受影响接口检查
5. 输出：
   - 修改文件列表
   - 新增对象与字段
   - 兼容性影响
   - 验证结果
