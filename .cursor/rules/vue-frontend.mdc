---
alwaysApply: true
---
技术栈：
- Vue 3
- TypeScript
- 组合式 API
- 页面与组件分层清晰

实现规则：
- 优先使用 <script setup lang="ts">
- 组件职责单一，页面负责组织，组件负责呈现
- 避免在页面文件中堆大量业务判断和样式细节
- 重复 UI 必须抽成组件
- 重复文案结构必须抽成配置或映射
- 状态命名表达业务含义，不用 data/list/info 这类模糊命名

结构建议：
- pages：页面编排
- components：复用组件
- composables：页面逻辑与状态组合
- constants/config：阶段文案、映射、静态配置
- types：明确的数据结构定义

代码要求：
- 保持 props、emits、computed、methods 命名清晰
- 删除无用样式、无用变量、无用注释
- 不要把“临时修复”留在最终代码中
- 不要硬编码魔法值，能抽离就抽离
- 响应式布局必须考虑常见桌面宽度和窄屏

前端质量要求：
- 改页面时同步检查视觉层级、文案、交互状态、空状态、加载状态、错误状态
- 优先保证页面完成度和一致性，再考虑装饰性优化