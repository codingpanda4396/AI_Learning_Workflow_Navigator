---
description: LLM 使用边界。约束本项目中大模型的职责、输入输出格式、提示词使用方式与降级策略。
alwaysApply: true
---

# LLM 在本项目中的定位

LLM 是系统的增强器，不是唯一大脑。

它主要负责：

- 解释增强
- 表达润色
- 任务执行阶段的导师对话脚手架
- 学习报告摘要与反馈文案
- 在受限候选集内做风格化生成

它不负责：

- 端到端自由决定主链路
- 自由生成核心业务状态
- 替代规则系统输出最终主决策

# 必须遵守的原则

1. **先结构，后生成**
   - 先由规则、枚举、候选、约束定义结构
   - 再让 LLM 生成解释和表达

2. **先候选，后选择**
   - 如果是规划、next action、风险判断等关键环节，优先使用候选集 + 受限选择

3. **先可回退，后接入**
   - 关键链路在 LLM 不可用时也应能退化运行

4. **先输入裁剪，后调用模型**
   - 只传当前任务真正需要的上下文
   - 不要把整个系统历史原样塞给 LLM

# Prompt 设计要求

所有 prompt 都应包含：

- 当前环节名称
- 当前目标摘要
- 当前用户状态摘要
- 允许使用的决策边界
- 明确输出格式
- 禁止行为

如果是结构化输出，必须显式要求 JSON 或固定字段格式。

# 输出纪律

LLM 输出必须满足：

- 与当前阶段职责一致
- 不越权修改主决策字段
- 不杜撰未提供的用户事实
- 不输出与当前任务无关的大段泛化内容
- 不把“建议语气”伪装成“已决策结果”

# 执行阶段导师要求

内嵌导师不是自由聊天机器人。

它必须围绕当前任务运行，并显式受以下上下文约束：

- 当前任务目标
- 当前任务类型
- 当前用户画像摘要
- 推荐提问方式
- 当前禁止事项

导师回复应优先：

- 引导用户表达
- 拆解问题
- 帮助用户复述
- 提供追问方向
- 检查理解缺口

不要直接把整套知识内容灌给用户。

# 报告与反馈要求

生成学习报告时：

- 先基于结构化证据形成结论
- 再调用 LLM 生成可读文案
- 文案必须与结构化结论一致

不要让 LLM 自由想象“为什么学得不好”或“下一步应该做什么”。

# 降级策略

如果 LLM 失败、超时或结果不可信：

- 保留结构化主结果
- 返回规则生成的默认解释文案
- 不阻断主链路

LLM 的失败不能让主流程失效。
