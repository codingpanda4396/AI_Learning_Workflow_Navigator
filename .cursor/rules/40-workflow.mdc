---
description: 开发工作流规则。约束 Cursor 在本项目中的任务拆分、计划方式、验证动作与输出格式。
alwaysApply: true
---

# 任务拆分原则

在本项目中，优先使用“小而完整”的任务，不要一次处理过大范围。

推荐任务粒度：

- 一个接口
- 一个页面
- 一个对象链路
- 一个模块中的一项明确改动
- 一个问题修复

避免一次同时修改：

- 前端 + 后端 + LLM + 文案 + 架构
- 多条链路的核心逻辑
- 尚未冻结 contract 的上下游模块

# 何时先出计划

满足以下任一条件时，先输出计划，再编码：

- 需要跨多个文件
- 需要跨前后端
- 需要调整对象结构
- 需要影响主链路 contract
- 需求表述较长或目标较复杂

计划至少包含：

1. 改动目标
2. 涉及文件
3. 新增 / 修改对象
4. 风险点
5. 验证方式

# 上下文读取要求

开始任务前：

- 先阅读相关模块文档和已有代码
- 先确认当前改动属于闭环哪一环
- 只引入必要上下文，避免无关文件污染

如果用户已经给出明确文件路径或文档，优先读取这些内容。

# 编码过程要求

实现时必须：

- 先复用现有模式，再新增模式
- 优先最小改动
- 保持命名、枚举、状态统一
- 不擅自引入与 MVP 无关的新层次、新框架、新抽象

# 验收要求

完成改动后，必须尽可能进行最小必要验证。

根据改动类型选择：

- 后端：编译、单测、受影响接口检查
- 前端：编译、路由可达、关键页面渲染
- 全链路：关键主路径手工验证

如果无法执行某项验证，要明确说明未验证项和原因。

# 输出格式要求

每次完成任务后，输出内容应尽量包括：

1. 本次做了什么
2. 改了哪些文件
3. 为什么这样改
4. 风险或未完成项
5. 如何验证

# 对话管理要求

- 同一对话尽量只围绕一个逻辑任务
- 任务切换时优先开启新对话
- 如果需要复用以前内容，优先引用已有文档或历史结论，不要重复长篇复制
- 当当前对话明显跑偏时，回到计划并重新开始

# 本项目的默认开发顺序

除非用户明确要求，否则优先顺序为：

1. 冻结对象、枚举、DTO、接口合同
2. 打通 mock / stub 最小闭环
3. 替换真实规则逻辑
4. 接入 LLM 增强解释和导师脚手架
5. 收口 UI 与展示体验

如果当前任务违反这个顺序，应先提醒可能的风险，再执行。
