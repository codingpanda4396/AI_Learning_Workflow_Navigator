---
description: Global design system for the frontend UI
alwaysApply: false
---
你正在维护一个“学习流程产品”的前端，不是内容展示站。

目标：
- 所有页面必须降低认知负担
- 用户首屏 1 秒内知道“现在该做什么”
- 风格统一优先于局部炫技

视觉系统约束：
- 优先使用统一设计 token，不允许随意硬编码颜色、圆角、阴影、间距
- 页面背景、卡片背景、边框、主文字、次文字、强调色必须保持稳定语义
- 圆角层级保持克制：页面容器、卡片、按钮需有一致的圆角体系
- 阴影只用于弱层级区分，不能堆叠制造噪音
- 间距必须形成稳定节奏：组件内间距 < 模块间间距 < 页面区块间间距
- 字号层级必须清晰：标题、阶段标题、正文、辅助信息、标签不可混乱

布局原则：
- 一个页面只允许一个 primary CTA
- 同一屏不允许超过 3 个同层级重点模块争抢注意力
- 优先纵向线性流程，而不是同时摊开大量信息
- 卡片只是承载信息，不是为了显得“丰富”

实现约束：
- 新增 UI 前先复用已有基础组件或抽象公共组件
- 不要为单页面写孤立样式，优先沉淀成可复用样式或组件
- 样式命名要表达语义，不要表达颜色或临时视觉印象